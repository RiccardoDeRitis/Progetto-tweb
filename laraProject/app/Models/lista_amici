<?php 
 
namespace App\Models;

class ListaAmici {

    protected $table = 'Lista_amici';
    protected $primaryKey = 'IDListaAmici';
    public $timestamps = false;

    // Relazione One-To-One con users
    public function listaUtente() {
        return $this->hasOne(users::class, 'id', 'id');
    }
    
}